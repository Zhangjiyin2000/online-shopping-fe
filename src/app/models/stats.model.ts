export interface PurchasedProductSummary {
  productId: number;
  name: string;
  totalQuantity: number;
}

export interface ProductProfitSummary {
  productId: number;
  name: string;
  totalProfit: number;
}